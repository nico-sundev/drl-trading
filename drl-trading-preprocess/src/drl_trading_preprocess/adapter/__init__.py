"""Adapter layer of the drl-trading-preprocess service.

This layer contains implementations of core ports (interfaces) and integrations
with external systems. Adapters implement core interfaces and depend on core layer.
"""
