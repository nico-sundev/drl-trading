# Environment variables for CI/CD operations
# Copy this file to .env and fill in your values
# Then run: source .env

# =============================================================================
# AWS Configuration (for CI/CD image in ECR)
# =============================================================================
# Required: Your AWS Account ID
export AWS_ACCOUNT_ID=123456789012

# Optional: AWS Region (defaults to us-east-1)
export AWS_REGION=us-east-1

# Optional: ECR Repository name (defaults to drl-trading-ci)
export ECR_REPOSITORY=drl-trading-ci

# Optional: Image tag (defaults to latest)
export CI_IMAGE_TAG=latest

# =============================================================================
# UV Source Overrides (Optional - for proprietary packages)
# =============================================================================
# By default, drl-trading-strategy-example is used from the workspace.
# To use a proprietary version from GitLab artifactory, uncomment:
# export UV_SOURCES__DRL_TRADING_STRATEGY_EXAMPLE='{ index = "gitlab-artifactory" }'

# Note: The artifactory must be configured in uv.toml
# Random users cloning the repo don't need this - workspace version works out of the box
